subprojects {
    repositories {
        mavenLocal()
    }

    group = "kr.starly.libs.nms.${project.name}"
    version = rootProject.version
}

project(":nms:common") {
    group = "kr.starly.libs.nms"
}