group = "kr.starly.server.nms.${project.name}"
version = rootProject.version

dependencies {
    compileOnly "org.spigotmc:spigot:1.20.6-R0.1-SNAPSHOT:remapped-mojang"
    compileOnly project(":nms:common")
}